<Window x:Class="Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="1000">

    <Grid>
        <Grid.Resources>
            <local:CurveInfo x:Key="Curves"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="22"></RowDefinition>
            <RowDefinition Name="gridRow" Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Name="gridColumn" Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ComboBox Name="Box" SelectionChanged = "ChangeGraphic" Grid.Row="0"
                  DataContext="{StaticResource Curves}" Panel.ZIndex="1"
                  SelectedItem="{Binding BoxCurve , Mode=TwoWay}" ItemsSource="{Binding ListCurves}">
        </ComboBox>
        <Path Name="graphicSystem" Grid.Row="1" Stroke="Black"/>
        <Path Name="graphic" Grid.Row="1" Stroke="DarkRed"/>
    </Grid>
</Window>
